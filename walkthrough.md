# 識生學坊 v3.0 Feature Walkthrough

本次更新大幅強化了數據分析與互動體驗，新增了 **Analytics Dashboard (數據儀表板)**、**Smart Auto-tagging (智慧標籤)** 與 **Gamification 2.0 (遊戲化升級)** 三大核心功能。

## 🐛 Bug Fixes & Refactoring (Phase 3.2)
- **Header Overlap Fix**: Resolved layout issues where the header overlapped content on Home and Dashboard screens by adjusting `pt` (padding-top) and `mt` (margin-top).
- **Runtime Error Fix**: Fixed `ReferenceError: StudentCard is not defined`.
    - **Cause**: Trying to use a `<StudentCard />` component that wasn't defined (logic was previously inline).
    - **Solution**: Extracted the student card UI into a reusable `StudentCard` component in `App.jsx`.
- **Logic Correction**: Moved "Smart Grouping" modal logic from `ClassManager` to `StudentManager` to access correct state variables.

## 1. Analytics Dashboard (戰績進化中樞)

位於「成長數據」頁面，現在提供了深入的學習分析：
- **成長趨勢 (Growth Trend)**：透過折線圖追蹤班級的平均分數變化，讓進步一目瞭然。
- **辨認難易度指數 (Difficulty Index)**：自動分析最常被認錯的學生 (準確率 < 80%)，幫助老師針對性加強記憶。
- **班級爭霸戰 (Leaderboard)**：在數據總覽頁面，顯示各班級的平均得分排行，激發良性競爭。

## 2. Smart Auto-tagging (智慧標籤管理)

在「班級編制」中，您現在可以更靈活地管理學生資料：
- **標籤編輯器**：點擊學生卡片左上角的標籤圖示，即可為學生添加特徵標籤 (如：眼鏡, 馬尾, 運動員)。
- **標籤篩選特訓**：在啟動練習前，使用新的標籤篩選器選擇特定標籤，系統將自動生成針對該特徵的專屬特訓 (如：「眼鏡」特訓)。
- **視覺化標籤**：學生卡片上會直接顯示前兩個標籤，方便快速識別。

## 3. Gamification 2.0 (遊戲化體驗升級)

練習模式變得更加刺激有趣：
- **Combo 連擊系統**：連續答對將觸發 Combo 特效，並獲得額外分數加成 (Score + Combo x 10)。
- **動態回饋**：答對時的視覺與數值回饋更加強烈。
- **最高連擊紀錄**：結算畫面新增「最高連擊」統計，挑戰您的專注極限。
- **記憶大師評級**：根據分數與連擊數，給予 S+/S/A/B 等級評價。

## 4. UI/UX Design Optimization (介面優化)

為了提升操作體驗與視覺質感，我們針對核心區塊進行了 **Premium Claymorphism** 設計升級：

- **Search & Tag Filter (搜尋與標籤)**：
    - **多彩漸層搜尋列**：使用玻璃擬態與動態光暈，讓搜尋功能更顯眼。
    - **水平捲動標籤列**：取代舊版下拉選單，改為直觀的水平滑動按鈕 (Clay Pills)，在手機上操作更流暢。
- **Batch Import (批次匯入)**：
    - **雙欄卡片設計**：將原本擁擠的介面改為清晰的雙欄佈局 (Excel / Photos)。
    - **直觀拖放區**：使用大型圖示與主題色 (Emerald/Blue) 區分功能，並加入互動懸浮效果。
- **Add Student (收編新戰友)**：
    - **深色沉浸式卡片**：使用 Indigo 漸層與玻璃擬態背景，讓「建立」動作更具儀式感。
    - **精緻輸入框**：加入浮動圖示 (User/Hash) 與光影效果。
    - **一鍵上傳**：整合照片上傳區，操作更直覺。
- **Student List (學員名單)**：
    - **高密度流體佈局**：採用 `Auto-fill Grid` 技術，自動根據螢幕寬度調整卡片數量，大幅減少留白，讓畫面更緊湊。
    - **懸浮卡片特效**：卡片縮小但資訊不減，滑鼠懸停時優雅上浮。
    - **極簡資訊呈現**：優化照片佔比，使用漸層文字覆蓋，在有限空間內保持清晰度。
- **Class Management (班級管理)**：
    - **深色漸層 Hero Card**：將「啟動新班級」作為視覺重心，使用深色漸層與裝飾性光暈，營造高級感。
    - **玻璃擬態班級卡**：現有班級採用懸浮、玻璃擬態設計，並加入 "Enter Class" 引導視覺。
- **Header & Profile (導航與個人檔案)**：
    - **玻璃膠囊導航 (Nav Capsule)**：全新的懸浮膠囊設計，背景模糊與漸層邊框，讓 Logo 更顯眼。
    - **迷你儀表板 (Mini-Dashboard)**：下拉選單升級為含個人資訊卡片的迷你面板，提供更直覺的操作入口。
    - **動態互動 (Micro-Interactions)**：Logo 閃光特效與頭像光環，增添細節質感。
    - **RWD 與層級優化**：修復了移動端內容重疊問題 (`pt-48`)，並提升下拉選單層級 (`z-[9999]`) 確保不被遮擋。
- **AI 記憶口訣 (AI Memory Anchors)**：
    - **快取機制**：實作 LocalStorage 快取，避免重複呼叫 Gemini API，節省成本並加速回應。
    - **教育提示詞**：優化 AI Prompt，產出更具教育意義且好記的特徵描述。
- **離線快取優化 (Offline Capability)**：
    - **PWA 支援**：配置 `vite-plugin-pwa`，支援應用程式安裝與離線存取。
    - **自動快取**：靜態資源與圖片將自動快取，提升弱網環境下的體驗。
- **UI/UX 優化 (Visual Refinement)**：
    - **下拉選單按鈕**：將原本單調的按鈕升級為 **Claymorphism 3D 風格**，使用漸層色與浮雕效果，提升點擊慾望。
    - **手機版適配**：針對小螢幕裝置優化下拉選單位置，確保內容置中且不被裁切。
    - **匯入指引 (Pro Max)**：
        - **3D 浮雕按鈕**：升級 `clay-upload-card` 為高互動性、漸層豐富的方塊按鈕。
        - **玻璃擬態說明**：新增 `glass-instruction-card`，將規則說明封裝在精美的毛玻璃卡片中，閱讀更舒適。
        - **RWD 優化**：手機版自動堆疊卡片，確保最佳操作體驗。
    - **按鈕優化**：
        - **隱藏預設元件**：美化所有上傳按鈕，強制隱藏瀏覽器預設樣式。
        - **精緻小按鈕**：新增 `btn-glass-pill` (說明開關) 與 `btn-square-danger` (刪除按鈕)，統一樣式語言。
    - **輸入框升級**：
        - **Clay Input Primary**：將「建立班級」輸入框改為高對比的 **白底玻璃擬態**，搭配內陰影與聚焦光暈，解決了原本黑底讀取困難的問題。
        - **圖示調色**：配合亮色背景，將輸入框圖示調整為深靛色，視覺更清晰。
    - **戰績頁面優化**：
        - **榮譽榜選單**：將戰績頁面的班級按鈕升級為 **Clay Pill (戰績膠囊)**，與主頁面風格統一。
        - **置中滾動容器**：RWD 優化，寬螢幕自動置中，手機版支援慣性滑動。
    - **Hero Section Optimization (Pro Max)**：
        - **Aurora Background**：動態生成的極光色塊 (Indigo/Rose/Cyan) 在背景中緩慢流動，營造夢幻氛圍。
        - **Glassmorphism Hero Card**：主視覺區採用高質感玻璃擬態卡片，搭配懸浮 3D 圖示 (`Users` + `Sparkles`)。
        - **Typography Upgrade**：標題使用 `.text-gradient-aurora` 漸層色，並引入 `clamp()` 技術實現完美的流體縮放 (Fluid Typography)。
        - **RWD Layout**：針對手機版優化了間距與 3D 元素尺寸，確保在任何裝置上都能完美呈現。
        - **Interactive Button**："開始奇幻特訓" 按鈕加入流光特效與 "Pro Max" 標籤，提升點擊誘因。
    - **Back Button Optimization (返回按鈕)**：
        - **Glassmorphism Style**：使用 `.btn-glass-secondary` 類別，賦予次級行動按鈕半透明玻璃質感與懸浮效果，取代原本單調的文字連結。
        - **User Experience**：加入 `ArrowLeft` 圖示並擴展點擊區域至全寬 (Full Width)，大幅提升手機操作的便利性。
    - **Search Bar Optimization (搜尋列 RWD)**：
        - **Tailwind CSS v4 Migration**：全面遷移至 **`@tailwindcss/vite`** 插件，並移除舊版 PostCSS 配置檔。此舉徹底解決了 Build Error 與樣式衝突，確保 Pro Max UI 穩定渲染。
        - **Pro Max UI Revamp**：
            - **Container**：升級為 `h-16 md:h-20` 的巨大化 Pill Shape 設計，搭配 `shadow-clay-card` 深度陰影與 `backdrop-blur-xl` 毛玻璃特效。
            - **Contrast**：邊框改為 `border-indigo-100` (Focus 時 `border-indigo-300`)，在淺色背景上更具層次感。
            - **Typography**：輸入文字放大至 `text-lg md:text-xl`，提升閱讀體驗與操作手感。
    - **Layout & Stability Fixes (版面與穩定性修正)**：
        - [x] **Header Overlap Fix**：修正了「啟動新班級」頁面的返回按鈕與頂部導航列重疊的問題。將按鈕改為 **左側對齊 (`justify-start`)**，避開了置中的 Header Capsule。
    - [x] **Score Visibility Optimization**：針對遊戲模式中的「得分顯示 (Trophy Pill)」，強制套用 **深色背景 (!bg-indigo-600)** 與 **白色文字 (!text-white)**，並加入 `SCORE` 標籤，大幅提升辨識度。
    - [x] **Hero Section Overlap**：修正了因為 Fixed Header 導致的內容遮擋問題，將主內容上方內距增加至 `pt-40 md:pt-52`，確保標題清晰可見。
        - **CSS Build Strategy**：將 Google Fonts 遷移至 `index.html`，解決了 PostCSS `@import` 排序衝突，確保 Tailwind Utility Classes 能正確載入。
    - **Student Grid UI Optimization (學員列表 Pro Max)**：
        - **Vibrant Gradients**：大頭照預設圖改為 `bg-gradient-to-br from-indigo-50 to-purple-50`，並在 Hover 時加深，消除原本蒼白的視覺感。
        - **Glassmorphic Cards**：卡片背景升級為 `bg-white/80 backdrop-blur-xl`，搭配 `border-4 border-white` 與 `shadow-clay-card`，營造強烈的立體懸浮感。
        - **Typography Enhancement**：姓名加粗為 `font-black text-xl`，座號改為 `bg-indigo-500 text-white` 的膠囊樣式，大幅提升可讀性與專業度。
        - **Fluid Grid**：將 Grid Columns 調整為 `minmax(160px, 1fr)`，讓卡片在各種螢幕尺寸下都能保持最佳比例與衝擊力。
    - **Import UI Compact Optimization (匯入按鈕優化)**：
        - **Horizontal Layout**：移除原本巨大的 `2:1` 比例限制，改為 **Flex Row 橫向排列**，節省垂直空間並提升資訊密度。
        - **Compact Dimensions**：移除 `min-height: 240px`，縮減 Padding 與 Icon 尺寸 (`80px` -> `64px`)，使其視覺上更輕盈精緻。
    - **Student Grid Polish (細節打磨)**：
        - **Text Overlap Fix**：修正了預設頭像與文字重疊的問題。將 Icon 上移 (`pb-8`)，並將文字層級提升 (`z-20`)，確保姓名與座號永遠清晰可見。
        - **Readability**：加強底部漸層高度 (`pt-24`)，讓白色文字在淺色背景上依然保有足夠的對比度。
    - **Runtime Fix (錯誤修正)**：
        - **Missing Import**: 修正了 `TagEditor` 中 `XCircle` 未引入導致的 `ReferenceError`，確保標籤編輯視窗能正常開啟與關閉。
    - **Student Details Modal (相片放大檢視)**：
        - **Split Layout**：將單純的標籤編輯器升級為 **左右分割佈局** (Mobile 為上下)，左側展示大尺寸學員照片，右側進行標籤管理。
        - **Interactive Zoom**：照片區域支援 Hover 放大，並可點擊開啟原圖，滿足使用者「看清楚大頭照」的需求。
        - **Visuals**：採用 `clay-card` 與 `backdrop-blur-xl`，維持全站一致的 Pro Max 質感。
        - **Desktop Optimization**：針對電腦版優化了視窗尺寸與雙欄比例 (40/60)，確保在寬螢幕下擁有更舒適的閱讀體驗與空間感。
        - **Layout Fix**：採用 **Fixed Header/Footer** 佈局，將標題與按鈕區塊固定，僅中間內容可滾動，確保重要操作按鈕永遠可見。

## 檔案變更列表

| 檔案 | 變更內容 |
| --- | --- |
| `src/App.jsx` | 實作新的搜尋列、標籤滑動列、批次匯入雙欄介面 |
| `src/index.css` | 新增 `clay-pill`, `clay-upload-card`, `scrollbar-hide` 等樣式類別 |
| `src/components/GameMode.jsx` | 實作 Combo 系統、分數加成邏輯、結算畫面更新 |
| `src/components/StatsView.jsx` | 實作班級排行榜、整合 AnalyticsDashboard |
| `src/components/AnalyticsDashboard.jsx` |新建 Recharts 圖表組件 |
| `src/hooks/useStore.js` | 新增 `tags` 欄位初始化、`updateStudentTags` 函數 |
| `src/hooks/useRecognitionStats.js` | 支援詳細遊戲數據記錄 (供未來進階分析使用) |

## 5. AI Memory Anchors (AI 記憶口訣)

運用 **Gemini Vision API** 多模態模型，賦予系統「看懂」學生的能力：

- **一鍵生成口訣**：在學生詳情頁面 (Student Details Modal)，點擊「✨ AI 記憶特徵」按鈕，AI 將自動分析照片特徵。
- **生動描述**：AI 會以繁體中文條列出 3 個最明顯的外觀特徵 (如：黑框眼鏡、燦爛笑容、馬尾)，幫助老師建立記憶點。
- **可編輯與儲存**：生成的描述會顯示在專屬的文字區域，老師可自由修改或補充，並隨標籤一同儲存至雲端 (Firestore)。
- **智慧快取**：實作 LocalStorage 快取機制，對同一張照片的分析結果進行暫存，大幅減少 API 呼叫次數並加速回應。

### 檔案變更追加
| 檔案 | 變更內容 |
| --- | --- |
| `src/hooks/useGeminiVision.js` | 新增：封裝 Gemini API 呼叫、快取邏輯與 Prompt Engineering |
| `src/hooks/useStore.js` | 新增：`updateStudentDescription` 用於儲存 AI 強化的描述 |
| `src/App.jsx` | 更新：`TagEditor` 整合 AI 按鈕、結果顯示區與編輯儲存邏輯 |

## 6. Phase 3.1: Intelligent Features & Stability (Current) 🎉

### ✨ Auto-Tagging Pipeline (自動化標籤)
- **AI 雙重分析**：Gemini Vision 現在除了生成描述外，還會自動提取 3-5 個關鍵字標籤 (Tags)。
- **一鍵整合**：前端 `TagEditor` 自動接收並填入新標籤，省去手動輸入的繁瑣。

### 👁️ Visual Regression Testing (視覺防護網)
- **自動快照**：透過 Playwright 建立 Landing Page 的基準截圖 (Baseline)。
- **變更偵測**：確保 UI 改動不會意外破壞版面結構。

### 📱 PWA Hardening (離線強化)
- **字型快取**：Google Fonts 改為 `StaleWhileRevalidate` 策略，加速載入並支援離線。
- **Native App 體驗**：設定 `display: standalone`，安裝後更像原生應用程式。

### 🎓 Smart Grouping Component (智慧分組助手)
- **多元策略**：
    - **🎲 隨機分組**：快速打散。
    - **⚡ 異質分組**：S型排列，強弱互補 (平衡戰力)。
    - **❤️ 興趣分組**：將相同標籤的學生聚在一起。
- **即時預覽**：調整人數與策略後，立即生成並展示分組結果卡片。

## 7. Phase 4: Advanced Interaction & Social (New) 🚀

### 🗣️ Voice Coach (語音記憶教練)
- **聽覺學習**：整合瀏覽器 `SpeechSynthesis` API，讓「阿凱老師」唸出學生姓名與 AI 記憶口訣。
- **自動播放**：在練習模式中可開啟「Auto-play」，答題時自動朗讀正確答案，強化記憶連結。
- **手動發音**：點擊卡片旁的喇叭圖示，隨時複習正確唸法與特徵描述。

### 📱 Social Review Cards (社交分享卡片)
- **戰績美圖**：一鍵生成精美的 Claymorphism 風格戰績卡，包含分數、評價與 QR Code。
- **多入口支援**：
    - **Dashboard**：分享最新的練習成長趨勢。
    - **Game Result**：特訓結束後立即分享高分榮耀。
- **技術實作**：使用 `html2canvas` 進行 DOM 截圖，支援高解析度下載。

### 🎨 Tag/Student Editor Optimization (Pro Max)
- **Claymorphism**：引入 `clay-card` 與 `clay-input`，讓標籤編輯器介面更具立體感與現代感。
- **Visual Hierarchy**：強化 AI 記憶口訣區塊的視覺層級，使用漸層邊框與圖示，引導使用者操作。
- **Fluid Response**：優化手機版佈局，確保在小螢幕上也能流暢操作。
- **Vibrant Colors**：使用 Indigo/Purple 漸層色系，呼應整體品牌風格。

## 🔮 Next Steps (Phase 5 Preview)
未來我們將邁向 **「生態系與智能化」**：
1. **🤖 AI 虛擬班級教練**：每週自動分析數據，提供教學建議。
2. **🌐 跨平台教具整合**：讓數據在不同教學工具間無縫流動。

> 詳細規劃請參考 `FUTURE_ROADMAP.md`。
