rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 已登入的使用者可以讀取和寫入 students 路徑下的所有檔案
    match /students/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
